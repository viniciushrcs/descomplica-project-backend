const repositoriesMock = require('./repositories')
const queriesMock = require('./queries')
const mutationsMock = require('./mutations')

module.exports = {
  repositoriesMock,
  queriesMock,
  mutationsMock,
}
